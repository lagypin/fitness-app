# TODO

Ideas for future work on the app. Not actively being worked on.

## 1. Make the app easy for non-coders to use/customize

Goal: let someone fork/clone the app and make it their own without touching code.

- Easy way to import an exercise library (e.g. from a JSON/CSV file, or an in-app import screen).
- Allow changing the exercise structure — the six categories I currently have are hard-coded. Let users swap them out for their own, or add/remove categories.
- Add a warm-up and cool-down exercise database. Right now warm-up/cool-down is redundant/missing.
- Have the app randomize warm-ups and cool-downs based on the type of workout selected.
- Easy way to customize theming without touching code:
  - color scheme
  - fonts
  - emojis (e.g. the category icons)

## 2. Fill the white space on the main page

- Add a quote of the day (or similar) to the bottom of the main page to use the empty space.

## 3. Weight recommendations on first-time setup

- During onboarding, let experienced lifters input their PRs for the main lifts.
- Use those PRs to suggest working weights for:
  - the main lifts themselves
  - accessory/secondary exercises (derived from main-lift numbers)
- Goal: new users get sensible starting weights instead of guessing.
